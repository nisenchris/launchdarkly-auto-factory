{
  "key": "auto-factory-approval-gates",
  "name": "AutoFactory approval gates",
  "description": "JSON array of agent node keys that require human approval before they run (per-step gates). Read natively by the runtime; default [] = no gates. Provisioned off (serves []); flip on to gate before the flag implementer, or edit the on-variation to gate other steps.",
  "kind": "multivariate",
  "temporary": false,
  "tags": ["auto-factory"],
  "variations": [
    { "value": [], "name": "No gates" },
    { "value": ["autofactory-flag-implementer"], "name": "Gate before flag implementer" }
  ],
  "defaults": { "onVariation": 1, "offVariation": 0 }
}
